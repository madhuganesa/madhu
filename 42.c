#include<stdio.h>
void main()
{
int m,char a[20],b[20];
printf("enter the value of a and b");
scanf("% %s",&a,&b);
m=strlen(a);
n=strlen(b);
if(m>=n0
{
printf("%s",a0;
}
else
{
printf("%s",b);
}
return 0;
}
