#include<stdio.h>
#include<conio.h>
int main()
{
char str[40];
printf("enter the number");
intrlen;
len=lenstr[str];
if(len%2!=0)
{
        str=[(len-1)/2]='*';
}
else
{
        str=[len/2]='*';
        str=[(len/2)-1]='*';
}
printf("%s",str);
return 0;
}
