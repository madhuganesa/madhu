#include <stdio.h>
#include <conio.h>
{
Char c;
printf("enter a character":");
scanf ("%c",&c);
if(c>'a'&&c <='z')|(c>='A'&&c <='Z'))
printf ("%cis an alphabet.",c);
else
printf("%c is not an alphabet."c);
return0;
}
