#include<stdio.h>
#include<conio.h>
int main()
{
int a=7,b,c;
printf("enter the number");
scanf("%d",&b);
if(a!=0)
{
if(c>a)
{
printf("multiply %d",c);
}
else
{
printf("no multiply %d",c);
}
}
return 0;
}
